import * as middleware from './middleware';
import * as images from './images';
import * as users from './users';
import * as common from './common';

const controllers = { middleware, images, users, common };

export default controllers;
